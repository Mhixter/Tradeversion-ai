[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["corepack enable", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run railway:build", "ls -R artifacts/api-server/dist"]

[start]
cmd = "pnpm run railway:start"
