[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @workspace/api-server build"

[deploy]
startCommand = "pnpm --filter @workspace/db push && pnpm --filter @workspace/api-server start"
