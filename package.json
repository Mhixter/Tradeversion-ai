{
  "name": "tradeversion-ai",
  "private": true,
  "scripts": {
    "railway:build": "pnpm --filter @workspace/tradevision run build && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/db run push",
    "railway:start": "pnpm --filter @workspace/api-server run start"
  }
}
